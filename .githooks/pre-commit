#!/bin/sh
# Regenerates CONTEXT-SIZE.md from the skills folder and stages it, so every
# commit carries the context size of the skills it contains.
set -e
cd "$(git rev-parse --show-toplevel)"
node scripts/context-size.mjs
git add CONTEXT-SIZE.md
